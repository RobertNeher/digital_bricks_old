// import 'package:flutter/material.dart';

class LogicPin {
  bool value = false; // The current logic state (True/High or False/Low)
  String? connectedToId; // ID of the pin it's connected to
  // ... other properties (e.g., position offset on the component)
}
